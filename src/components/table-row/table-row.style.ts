import styled from "styled-components";

export const TableRowStyle = styled.tr`
  cursor: context-menu;
  .class {
    font-weight: bold;
    font-style: italic;
    font-size: 0.8rem;
  }
  .DP::after{
    content: ' (DP)'
  }
`;
